# Configuración explícita del proyecto: prioriza el comportamiento estándar de npm
# sobre valores heredados de ~/.npmrc (por ejemplo otro registry o legacy-peer-deps).
registry=https://registry.npmjs.org/
legacy-peer-deps=false
